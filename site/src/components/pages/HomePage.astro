---
// トップページ本体（§6.1）：S1〜S15。/（ja）と /en/（en）の両ルートから lang を受けて描画。
import BaseLayout from '../../layouts/BaseLayout.astro';
import CTAButton from '../CTAButton.astro';
import SectionHeading from '../SectionHeading.astro';
import ImagePlaceholder from '../ImagePlaceholder.astro';
import DiscoveryFigure from '../DiscoveryFigure.astro';
import LoopFigure from '../LoopFigure.astro';
import FunnelFigure from '../FunnelFigure.astro';
import PlanCard from '../PlanCard.astro';
import Stepper from '../Stepper.astro';
import Accordion from '../Accordion.astro';
import CTABand from '../CTABand.astro';
import { siteCommon } from '../../content/site';
import { copy } from '../../content/copy';
import { plans, planNotes } from '../../content/plans';
import { faqCategories } from '../../content/faq';
import { works } from '../../content/works';
import { ui } from '../../content/ui';
import { localizePath } from '../../i18n/utils';
import type { Lang } from '../../i18n/config';

interface Props {
  lang: Lang;
}
const { lang } = Astro.props;
const c = copy[lang];
const u = ui[lang].home;

const meta = {
  ja: {
    title: `ゴールドコーストの美容サロン集客・Instagram運用｜${siteCommon.brandName}`,
    description:
      'ゴールドコーストの日本人オーナー美容サロン（ネイル・まつげ・眉・ヘア・エステ）向けに、Web・Instagram・Google/MEO・レビュー獲得まで集客の仕組みを一貫サポート。日本品質の集客を、現地で信頼される形でお届けします。',
  },
  en: {
    title: `Beauty salon marketing & Instagram on the Gold Coast｜${siteCommon.brandName}`,
    description:
      'Japanese-quality marketing, delivered with local trust. For Japanese-owned beauty salons on the Gold Coast — nails, lashes, brows, hair and skin: web, Instagram, Google/MEO and reviews, run as one connected growth system.',
  },
}[lang];

const featuredFaq = faqCategories[lang]
  .flatMap((cat) => cat.items)
  .filter((i) => i.featured)
  .slice(0, 6);

// S1 ヒーロー背景スライドショー（3枚クロスフェード）：サロン / 美容 / ゴールドコーストの雰囲気
const heroSlides = [
  'https://images.unsplash.com/photo-1604654894610-df63bc536371?w=1800&q=80&fit=crop',
  'https://images.unsplash.com/photo-1487412947147-5cebf100ffc2?w=1800&q=80&fit=crop',
  'https://images.unsplash.com/photo-1522337360788-8b13dee7a37e?w=1800&q=80&fit=crop',
];

// 比較表のマーク表示設定
const markStyles = {
  yes: { glyph: '●', cls: 'text-brand', sr: c.comparison.legend.yes },
  partial: { glyph: '△', cls: 'text-accent', sr: c.comparison.legend.partial },
  no: { glyph: '×', cls: 'text-ink-muted/40', sr: c.comparison.legend.no },
} as const;
---

<BaseLayout lang={lang} title={meta.title} description={meta.description}>
  <!-- S1. ヒーロー（背景3枚クロスフェード＋黒オーバーレイ） -->
  <section class="hero relative isolate flex min-h-[92vh] items-center overflow-hidden">
    <!-- 背景スライドショー -->
    <div class="absolute inset-0 -z-20" aria-hidden="true">
      {
        heroSlides.map((src, i) => (
          <img
            src={src}
            alt=""
            data-hero-slide
            class:list={[
              'hero-slide absolute inset-0 h-full w-full object-cover',
              i === 0 ? 'opacity-100' : 'opacity-0',
            ]}
            loading={i === 0 ? 'eager' : 'lazy'}
            decoding="async"
            fetchpriority={i === 0 ? 'high' : undefined}
          />
        ))
      }
    </div>
    <!-- 黒の透明オーバーレイ（文字を浮き上がらせる） -->
    <div
      class="absolute inset-0 -z-10 bg-gradient-to-br from-black/75 via-black/55 to-black/30"
      aria-hidden="true"
    ></div>
    <div class="absolute inset-0 -z-10 bg-black/15" aria-hidden="true"></div>

    <div class="mx-auto w-full max-w-[1200px] px-5 pb-28 pt-36 md:pb-32 md:pt-44 lg:px-8">
      <div class="max-w-[720px]">
        <span class="inline-flex items-center gap-2 rounded-full border border-white/25 bg-white/10 px-4 py-1.5 text-xs font-medium tracking-wide text-white backdrop-blur">
          <span class="h-1.5 w-1.5 rounded-full bg-accent" aria-hidden="true"></span>
          {c.hero.eyebrow}
        </span>

        <h1 class="mt-6 text-[34px] leading-[1.22] text-white drop-shadow-sm md:text-[48px] lg:text-[56px]">
          <span class="block">{c.hero.heading.lead}</span>
          <span class="mt-1 block w-fit underline decoration-accent decoration-[3px] underline-offset-[10px]">
            {c.hero.heading.accent}
          </span>
        </h1>

        <p class="mt-6 max-w-[560px] text-[17px] leading-relaxed text-white/85">{c.hero.sub}</p>

        <ul class="mt-7 flex flex-wrap gap-2.5">
          {
            c.hero.micro.map((m) => (
              <li class="inline-flex items-center gap-1.5 rounded-full border border-white/20 bg-white/10 px-3.5 py-1.5 text-[13px] font-medium text-white backdrop-blur">
                <svg class="h-3.5 w-3.5 shrink-0 text-accent" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true">
                  <path d="m5 13 4 4L19 7" />
                </svg>
                {m}
              </li>
            ))
          }
        </ul>

        <div class="mt-9 flex flex-col gap-4 sm:flex-row sm:items-center">
          <CTAButton href={c.hero.ctaPrimary.href} lang={lang} variant="primary" ctaLocation="hero" class="px-8">
            {c.hero.ctaPrimary.label}
          </CTAButton>
          <CTAButton
            href={c.hero.ctaSecondary.href}
            lang={lang}
            variant="text"
            ctaLocation="hero-secondary"
            class="!text-white decoration-white/50 hover:!text-white/80"
          >
            {c.hero.ctaSecondary.label} →
          </CTAButton>
        </div>
      </div>
    </div>

    <!-- スライドインジケーター -->
    <div class="absolute bottom-6 left-1/2 z-10 flex -translate-x-1/2 gap-2.5 lg:bottom-8" aria-hidden="true">
      {
        heroSlides.map((_, i) => (
          <span
            data-hero-dot
            class:list={[
              'h-1.5 rounded-full bg-white/40 transition-all duration-500',
              i === 0 ? 'w-7 bg-white' : 'w-1.5',
            ]}
          />
        ))
      }
    </div>
  </section>

  <!-- S2. 信頼バー（実績サマリー＋雰囲気ギャラリー） -->
  <section class="relative overflow-hidden border-y border-line bg-gradient-to-b from-surface to-canvas px-5 py-16 md:py-20 lg:px-8">
    <!-- 上端の装飾アクセントライン -->
    <div class="absolute inset-x-0 top-0 h-px bg-gradient-to-r from-transparent via-accent/60 to-transparent" aria-hidden="true"></div>

    <div class="mx-auto max-w-[1200px]">
      <!-- 実績サマリー -->
      <dl class="grid gap-y-9 sm:grid-cols-2 lg:grid-cols-4 lg:gap-y-0">
        {
          c.trustBar.items.map((item, i) => (
            <div
              class:list={[
                'reveal px-2 text-center lg:px-8',
                i > 0 && 'lg:border-l lg:border-line',
              ]}
            >
              <dt class="flex items-center justify-center gap-2 text-[11px] font-medium uppercase tracking-[0.18em] text-accent">
                <span class="h-px w-4 bg-accent/40" aria-hidden="true" />
                {item.label}
                <span class="h-px w-4 bg-accent/40" aria-hidden="true" />
              </dt>
              <dd class="mt-3 font-display text-[19px] leading-snug text-ink md:text-[20px]">{item.value}</dd>
            </div>
          ))
        }
      </dl>

      <!-- 雰囲気ギャラリー -->
      <div class="reveal mt-14 grid grid-cols-2 gap-3 sm:gap-4 lg:grid-cols-4">
        {
          [1, 2, 3, 4].map((i) => (
            <div class="group relative overflow-hidden rounded-2xl shadow-sm ring-1 ring-line/80 transition-shadow duration-300 hover:shadow-lg">
              <ImagePlaceholder
                slot_id={`logo-strip-${i}`}
                label={u.logoLabel(i)}
                ratio="4/5"
                class="!rounded-none transition-transform duration-700 ease-out group-hover:scale-[1.06]"
              />
              <div class="pointer-events-none absolute inset-0 bg-gradient-to-t from-black/25 via-transparent to-transparent" aria-hidden="true" />
              <div class="pointer-events-none absolute inset-0 rounded-2xl ring-1 ring-inset ring-white/10" aria-hidden="true" />
            </div>
          ))
        }
      </div>
    </div>
  </section>

  <!-- S3. 課題提起 -->
  <section class="px-5 py-20 md:py-28 lg:px-8">
    <div class="mx-auto max-w-[1200px]">
      <SectionHeading label={c.problem.label} heading={c.problem.heading} lead={c.problem.lead} />
      <div class="mt-12 grid gap-6 sm:grid-cols-2 lg:grid-cols-4">
        {
          c.problem.items.map((item) => (
            <div class="reveal rounded-lg border border-line bg-surface p-6">
              <h3 class="font-sans text-base font-semibold">{item.title}</h3>
              <p class="mt-3 text-sm text-ink-muted">{item.body}</p>
            </div>
          ))
        }
      </div>
    </div>
  </section>

  <!-- S4. 豪州での「探され方」 -->
  <section class="bg-surface-cream px-5 py-20 md:py-28 lg:px-8">
    <div class="mx-auto max-w-[1200px]">
      <SectionHeading label={c.discovery.label} heading={c.discovery.heading} lead={c.discovery.lead} />
      <div class="reveal mt-12">
        <DiscoveryFigure lang={lang} />
      </div>
      <ul class="reveal mx-auto mt-10 max-w-[760px] space-y-3">
        {
          c.discovery.points.map((p) => (
            <li class="flex items-start gap-3 text-[15px] text-ink-muted">
              <svg class="mt-1.5 h-4 w-4 shrink-0 text-brand" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
                <path d="m5 13 4 4L19 7" />
              </svg>
              {p}
            </li>
          ))
        }
      </ul>
    </div>
  </section>

  <!-- S5. 解決策＝「輪」を回す -->
  <section class="px-5 py-20 md:py-28 lg:px-8">
    <div class="mx-auto max-w-[1200px]">
      <SectionHeading label={c.loop.label} heading={c.loop.heading} lead={c.loop.lead} />
      <div class="reveal mt-12">
        <LoopFigure lang={lang} />
      </div>
      <p class="reveal mx-auto mt-8 max-w-[760px] text-center text-[15px] text-ink-muted">{c.loop.note}</p>
    </div>
  </section>

  <!-- S6. 選ばれる理由（比較表） -->
  <section class="bg-brand-soft px-5 py-20 md:py-28 lg:px-8">
    <div class="mx-auto max-w-[1200px]">
      <SectionHeading
        label={c.comparison.label}
        heading={c.comparison.heading}
        lead={c.comparison.lead}
        align="center"
      />

      <!-- 比較表（横スクロール対応） -->
      <div class="reveal mt-12 overflow-x-auto rounded-2xl ring-1 ring-line/70">
        <table class="w-full min-w-[680px] border-collapse bg-surface text-left">
          <thead>
            <tr class="border-b border-line">
              <th scope="col" class="sticky left-0 z-10 bg-surface px-5 py-4 text-sm font-semibold text-ink">
                {c.comparison.label}
              </th>
              {
                c.comparison.conditions.map((cond) => (
                  <th scope="col" class="px-3 py-4 text-center align-bottom text-xs font-medium leading-snug text-ink-muted">
                    {cond}
                  </th>
                ))
              }
            </tr>
          </thead>
          <tbody>
            {
              c.comparison.rows.map((row) => (
                <tr
                  class:list={[
                    'border-b border-line/70 last:border-0',
                    row.us ? 'bg-brand text-white' : 'hover:bg-surface-cream/60',
                  ]}
                >
                  <th
                    scope="row"
                    class:list={[
                      'sticky left-0 z-10 px-5 py-4 text-sm font-medium',
                      row.us ? 'bg-brand text-white' : 'bg-surface text-ink',
                    ]}
                  >
                    {row.us && (
                      <span class="mr-1.5 inline-block rounded-full bg-accent px-2 py-0.5 text-[10px] font-bold tracking-wide text-white align-middle">
                        US
                      </span>
                    )}
                    {row.name}
                  </th>
                  {
                    row.marks.map((mark) => {
                      const m = markStyles[mark];
                      return (
                        <td class="px-3 py-4 text-center">
                          <span class:list={['text-lg leading-none', row.us ? 'text-white' : m.cls]}>
                            {m.glyph}
                          </span>
                          <span class="sr-only">{m.sr}</span>
                        </td>
                      );
                    })
                  }
                </tr>
              ))
            }
          </tbody>
        </table>
      </div>

      <!-- 凡例 -->
      <div class="reveal mt-6 flex flex-wrap items-center justify-center gap-x-6 gap-y-2 text-sm text-ink-muted">
        <span class="inline-flex items-center gap-1.5"><span class="text-brand">●</span>{c.comparison.legend.yes}</span>
        <span class="inline-flex items-center gap-1.5"><span class="text-accent">△</span>{c.comparison.legend.partial}</span>
        <span class="inline-flex items-center gap-1.5"><span class="text-ink-muted/40">×</span>{c.comparison.legend.no}</span>
      </div>

      <p class="reveal mx-auto mt-8 max-w-[760px] text-center text-[15px] leading-relaxed text-ink-muted">
        {c.comparison.note}
      </p>
    </div>
  </section>

  <!-- S7. サービス内容 -->
  <section id="service" class="px-5 py-20 md:py-28 lg:px-8">
    <div class="mx-auto max-w-[1200px]">
      <SectionHeading label={c.services.label} heading={c.services.heading} lead={c.services.lead} />
      <div class="reveal mt-12">
        <FunnelFigure lang={lang} />
      </div>
      <p class="reveal mt-8 rounded-lg bg-surface-cream px-6 py-5 text-sm text-ink-muted">{c.services.note}</p>
      <div class="reveal mt-8 text-center">
        <CTAButton href="/service" lang={lang} variant="text" ctaLocation="service-more">{u.serviceMore}</CTAButton>
      </div>
    </div>
  </section>

  <!-- S8. 料金プラン -->
  <section class="bg-surface-cream px-5 py-20 md:py-28 lg:px-8">
    <div class="mx-auto max-w-[1200px]">
      <SectionHeading label={u.pricingLabel} heading={u.pricingHeading} lead={u.pricingLead} align="center" />
      <div class="mt-14 grid gap-8 lg:grid-cols-3 lg:gap-6">
        {plans[lang].map((plan) => <PlanCard plan={plan} lang={lang} />)}
      </div>
      <div class="reveal mt-8 space-y-1 text-center text-sm text-ink-muted">
        {planNotes[lang].map((n) => <p>{n}</p>)}
      </div>
      <div class="reveal mt-6 text-center">
        <CTAButton href="/pricing" lang={lang} variant="text" ctaLocation="pricing-more">{u.pricingMore}</CTAButton>
      </div>
    </div>
  </section>

  <!-- S9. 導入の流れ -->
  <section class="px-5 py-20 md:py-28 lg:px-8">
    <div class="mx-auto max-w-[1200px]">
      <SectionHeading label={c.steps.label} heading={c.steps.heading} align="center" />
      <div class="mt-12">
        <Stepper items={c.steps.items} />
      </div>
    </div>
  </section>

  <!-- S10. 実績・事例 -->
  <section class="bg-surface px-5 py-20 md:py-28 lg:px-8">
    <div class="mx-auto max-w-[1200px]">
      <SectionHeading label={u.worksLabel} heading={u.worksHeading} lead={u.worksLead} />
      <div class="mt-12 grid gap-8 md:grid-cols-2">
        {
          works[lang].map((work) => (
            <a href={localizePath(`/works/${work.slug}`, lang)} class="reveal group block rounded-lg border border-line bg-canvas p-6 transition hover:border-brand">
              <ImagePlaceholder slot_id={work.thumbSlot} label={u.thumbLabel(work.name)} ratio="4/3" />
              <p class="mt-5 text-xs tracking-widest text-ink-muted">{work.industry}｜{work.area}</p>
              <h3 class="mt-1 font-sans text-lg font-semibold transition group-hover:text-brand">{work.name}</h3>
              <p class="mt-2 text-sm text-ink-muted">{work.summary}</p>
            </a>
          ))
        }
      </div>
      <div class="reveal mt-10 text-center">
        <CTAButton href="/works" lang={lang} variant="secondary" ctaLocation="works-more">{u.worksMore}</CTAButton>
      </div>
    </div>
  </section>

  <!-- S11. お客様の声 -->
  <section class="px-5 py-20 md:py-28 lg:px-8">
    <div class="mx-auto max-w-[1200px]">
      <SectionHeading label={c.testimonials.label} heading={c.testimonials.heading} align="center" />
      <div class="mt-12 grid gap-6 md:grid-cols-2">
        {
          c.testimonials.items.map((t, i) => (
            <figure class="reveal rounded-lg border border-line bg-surface p-7">
              <blockquote class="text-[15px] text-ink-muted">{t.comment}</blockquote>
              <figcaption class="mt-5 flex items-center gap-4">
                <ImagePlaceholder slot_id={`testimonial-avatar-${i + 1}`} label={u.avatarLabel} ratio="1/1" class="w-14 shrink-0 rounded-full" />
                <div>
                  <p class="text-sm font-medium">{t.name}</p>
                  <p class="text-xs text-ink-muted">{t.shop}</p>
                </div>
              </figcaption>
            </figure>
          ))
        }
      </div>
    </div>
  </section>

  <!-- S12. チーム紹介 -->
  <section class="bg-surface-cream px-5 py-20 md:py-28 lg:px-8">
    <div class="mx-auto max-w-[1200px]">
      <SectionHeading label={c.team.label} heading={c.team.heading} lead={c.team.lead} align="center" />
      <div class="mx-auto mt-12 grid max-w-[880px] gap-8 md:grid-cols-2">
        {
          c.team.members.map((member) => (
            <div class="reveal rounded-lg bg-surface p-7 text-center">
              <ImagePlaceholder slot_id={member.slot} label={u.portraitLabel(member.role)} ratio="1/1" class="mx-auto w-40 rounded-full" />
              <h3 class="mt-5 font-sans text-lg font-semibold">{member.name}</h3>
              <p class="mt-1 text-sm text-brand">{member.role}</p>
              <p class="mt-1 text-xs text-ink-muted">{member.location}</p>
              <p class="mt-4 text-sm text-ink-muted">{member.message}</p>
            </div>
          ))
        }
      </div>
      <div class="reveal mt-10 text-center">
        <CTAButton href="/about" lang={lang} variant="text" ctaLocation="about-more">{u.aboutMore}</CTAButton>
      </div>
    </div>
  </section>

  <!-- S13. FAQ（抜粋） -->
  <section class="px-5 py-20 md:py-28 lg:px-8">
    <div class="mx-auto max-w-[880px]">
      <SectionHeading label={u.faqLabel} heading={u.faqHeading} align="center" />
      <div class="reveal mt-12">
        <Accordion items={featuredFaq} analyticsPrefix="top" />
      </div>
      <div class="reveal mt-8 text-center">
        <CTAButton href="/faq" lang={lang} variant="text" ctaLocation="faq-more">{u.faqMore}</CTAButton>
      </div>
    </div>
  </section>

  <!-- S14. 最終CTAバンド -->
  <CTABand
    lang={lang}
    heading={c.finalCta.heading}
    sub={c.finalCta.sub}
    ctaLabel={c.finalCta.cta.label}
    ctaHref={c.finalCta.cta.href}
    ctaLocation="final-band"
  />
</BaseLayout>

<style>
  /* ヒーロー背景のクロスフェード */
  .hero-slide {
    transition: opacity 1400ms ease-in-out;
    will-change: opacity;
  }
  @media (prefers-reduced-motion: reduce) {
    .hero-slide {
      transition: none;
    }
  }
</style>

<script>
  // S1 ヒーロー：背景3枚を一定間隔でクロスフェード
  function initHeroSlideshow() {
    const hero = document.querySelector('.hero');
    if (!hero) return;
    const slides = Array.from(hero.querySelectorAll<HTMLElement>('[data-hero-slide]'));
    const dots = Array.from(hero.querySelectorAll<HTMLElement>('[data-hero-dot]'));
    if (slides.length < 2) return;
    if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) return;

    let current = 0;
    const INTERVAL = 5500;

    setInterval(() => {
      const next = (current + 1) % slides.length;
      slides[current].classList.replace('opacity-100', 'opacity-0');
      slides[next].classList.replace('opacity-0', 'opacity-100');
      dots[current]?.classList.remove('w-7', 'bg-white');
      dots[current]?.classList.add('w-1.5');
      dots[next]?.classList.remove('w-1.5');
      dots[next]?.classList.add('w-7', 'bg-white');
      current = next;
    }, INTERVAL);
  }

  initHeroSlideshow();
  document.addEventListener('astro:page-load', initHeroSlideshow);
</script>
